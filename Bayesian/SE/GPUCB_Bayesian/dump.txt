model.covar_module.base_kernel._set_lengthscale(torch.tensor([0.1]))
for i in range(safe_set.size(0)):
    if safe_set[i] == True & max_set[i] == True:
        x0 = test_x[i].item()
        break

x0_max = torch.tensor([[[x0]]])

x0_exp = torch.tensor([[[2.2, 1.9]]])

